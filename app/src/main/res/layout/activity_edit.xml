<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activity.EditActivity">


    <EditText
        android:id="@+id/activity_edit_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="@string/hint_name"
        android:inputType="textCapWords"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="Autofill" />
    <EditText
        android:id="@+id/activity_edit_telephone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="@string/hint_telephone"
        android:inputType="phone"
        app:layout_constraintTop_toBottomOf="@id/activity_edit_name"
        tools:ignore="Autofill" />
    <EditText
        android:id="@+id/activity_edit_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="@string/hint_e_mail"
        android:inputType="textEmailAddress"
        app:layout_constraintTop_toBottomOf="@id/activity_edit_telephone"
        tools:ignore="Autofill" />
    <Button
        android:id="@+id/activity_edit_save_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:text="@string/update_button"
        app:layout_constraintTop_toBottomOf="@id/activity_edit_email"/>

</androidx.constraintlayout.widget.ConstraintLayout>